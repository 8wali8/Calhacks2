# ⚠️ HACKATHON ONLY - These keys are exposed in the browser
# DO NOT use this pattern in production. All NEXT_PUBLIC_* vars are sent to the client.

# Required: Your Tavus API key (get from https://platform.tavus.io)
NEXT_PUBLIC_TAVUS_API_KEY=your_api_key_here

# Optional: Reuse an existing persona instead of creating a new one each run
# NEXT_PUBLIC_TAVUS_PERSONA_ID=p1234567890

# Optional: Specify a replica ID for the conversation
# NEXT_PUBLIC_TAVUS_REPLICA_ID=r1234567890
